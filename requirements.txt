PySide6>=6.5.0
pdfplumber>=0.10.0
openpyxl>=3.1.0
Pillow>=10.0.0
reportlab>=4.0.0
python-dateutil>=2.8.0

# Optional: enables PDF receipt preview in the output PDF
# Requires poppler to be installed:
#   Windows: https://github.com/oschwartz10612/poppler-windows/releases
#   macOS:   brew install poppler
#   Linux:   sudo apt install poppler-utils
# pdf2image>=1.16.0

# Optional: OCR-based amount extraction in Step 3 (better receipt matching in Step 4)
# easyocr  – pure Python, no system install needed, downloads ~1.5 GB model on first use:
#   pip install easyocr
# Alternative: pytesseract requires Tesseract installed system-wide:
#   pip install pytesseract
# easyocr>=1.7.0
# pytesseract>=0.3.10
